use std::collections::HashMap;

pub mod user_file_handler {
    fn check_user_file(user_file: &str) {

    }
        
    fn update_user_file(user_file: &str, user_map: HashMap<&str, Vec<&str, &str, &bool>>) {
        
    }

    fn get_user_map(user_file: &str) -> HashMap<&str, Vec<&str, &str, &bool>> {

    }

    fn add_user(user: &str, pass_hash: &str, privileges: &str, user_map: HashMap<&str, Vec<&str, &str, &bool>>) {

    }

    fn modify_user(user: &str, modify_param: uint, new_value: &str, user_map: HashMap<&str, Vec<&str, &str, &bool>>) {

    }

    fn delete_user(user: &str, user_map: HashMap<&str, Vec<&str, &str, &bool>>) {

    }
}

pub mod audit_handler {

}

pub mod event_handler {

}